export enum SaveRequestDraft {
    product = ".btn-draft",
    oneOffService = `[data-cy="save-draft"]`,
    recurringService = `[data-cy="save-draft"]`,
}

export enum SaveRequestSubmit {
    product = `.btn-success`,
    oneOffService = `[data-cy="submit_request"]`,
    recurringService = `[data-cy="submit_request"]`,
}