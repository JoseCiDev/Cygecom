export enum IsComexImportProduct {
    yes = '[data-cy="is-comex"]',
    no = '[data-cy="is-not-comex"]',
}

export enum IsComexImportService {
    yes = '[data-cy="is_comex_true"]',
    no = '[data-cy="is_comex_false"]',
}